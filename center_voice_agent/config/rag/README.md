# Методички для rag_search (keyword-поиск по .md/.txt)

Положите сюда файлы центра. Инструмент `rag_search` включите через `RAG_SEARCH_ENABLED=true`
и укажите `RAG_DOCS_DIR` (по умолчанию — этот каталог при запуске из корня проекта).

Пример: `sample_methodology.md`.

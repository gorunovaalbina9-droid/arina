МИНУСЫ И ЧТО НАДО ДОДЕЛАТЬ — center_voice_agent
(по code review, май 2026; обновлено после закрытия блоков 8–35)

================================================================================
ОРГАНИЗАЦИЯ КОДА
================================================================================

1. Всё в одном пакете — api/ и voice/integration выделены; CLI отдельно.
   [частично закрыто]

2. Два мира БД — runtime async + sync publish; риск блокировок остаётся.
   [P1: единый async publish]

3. AppContainer на процесс — get_process_container() для API/voice.
   [✅ P0 закрыто]

4. Rate limit Redis + LangGraph без singleton.
   [✅ закрыто]

5. Tools через registry — tools/registry.py, MCP stdio.
   [✅ закрыто]

6. Мёртвая таблица scenario_definitions — legacy 006, не используется в runtime.
   [документировано в db/migrations/README.md]

7. DX pytest — pythonpath в pyproject; запуск из корня center_voice_agent.


================================================================================
АРХИТЕКТУРА И ДОМЕН (8–15) — ЗАКРЫТО
================================================================================

8.  AgentGateway — prefetch/метрики в orchestration/prefetch.py, advance в coordinator.
9.  HTTP/WebSocket — api/routes/turn.py, ws.py; docs/API.md.
10. LLMProvider — agent/llm_provider.py.
11. Coordinator — явные repos, не gateway.session_messages_repository.
12. Смена режима — LLM + system_note при отказе; NLU modes/nlu.py (п.33).
13. Сценарии — keyword + mood/state_json переходы.
14. state_json — используется в session_state и переходах.
15. STM — 15 пар ходов (ShortTermMemory max_turns=15).


================================================================================
БЕЗОПАСНОСТЬ И COMPLIANCE (16–20) — ЗАКРЫТО
================================================================================

16. Модерация — regex, HTTP API, эскалация (moderation_engine.py).
17. Rate limit — Redis опционально (rate_limit_redis.py).
18. web_search — выключен по умолчанию; whitelist режимов.
19. Redact tool args — security/redact.py.
20. 152-ФЗ — docs/compliance/152FZ.md, consent hook, COMPLIANCE_LLM_REGION.


================================================================================
ИНФРАСТРУКТУРА И ДАННЫЕ (21–24) — ЗАКРЫТО
================================================================================

21. schema_migrations + безопасный SQL split (migrations_runner.py).
22. 003 — таблица учёта, не пропуск (db/migrations/README.md).
23. postgresql/ и sqlite/ каталоги миграций.
24. POST /admin/reload + composition/reload.py.


================================================================================
LLM И ИНСТРУМЕНТЫ (25–27) — ЗАКРЫТО
================================================================================

25. text_tool_fallback — режим off по умолчанию (settings).
26. StaticChatModel tool_calls + tests/test_static_llm_tools.py.
27. LangGraph compile_llm_tool_graph() без singleton.


================================================================================
ПРОДУКТ И SCOPE (28–35) — ЗАКРЫТО / ЗАДОКУМЕНТИРОВАНО
================================================================================

28. STT/TTS — вне пакета; docs/VOICE_INTEGRATION.md + integration/bridge.py.
29. Веб / ЛК — out of scope; docs/SCOPE.md.
30. RAG — keyword stub rag_search + config/rag/ (RAG_SEARCH_ENABLED=false).
31. MCP — tools/registry.py + center-agent-mcp + docs/MCP_TOOLS.md.
32. PostgreSQL SQL + Alembic baseline; docs/DATABASE.md.
33. NLU смены режима — modes/nlu.py (exact + fuzzy difflib).
34. HTTP API — POST /turn, WS /ws/session, GET /info; docs/API.md.
35. Пилот — docs/pilot/PILOT_READY.md (P0 чек-лист после security/infra).


================================================================================
ПРИОРИТЕТ: ДАЛЬШЕ (P2+)
================================================================================

- Единый async publish режимов/сценариев
- Векторный RAG и ingestion методичек
- Веб-кабинет родителя (отдельный репо)
- Смысловые переходы сценариев через LLM
- Полноценная ML-модерация продукта

================================================================================
Путь к проекту на ноутбуке:
c:\Users\Альбина\глебебля2\center_voice_agent
================================================================================
